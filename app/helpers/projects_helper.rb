module ProjectsHelper

  # 現在ログイン中のユーザーを返す (いる場合)
  # def current_project
  #   @current_project ||= Project.find_by(id: project[:id])
  # end
  #
  # def current_project?
  #   @current_project == Project.find_by(id: project[:id])
  # end

end
